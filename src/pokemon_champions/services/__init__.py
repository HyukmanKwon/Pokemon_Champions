"""비즈니스 로직.

두 종류가 섞여 있고, 구분해서 읽어야 한다.

  순수 계산 (stats.py, damage.py, modifiers.py, residual.py)
      conn 을 받지 않는다. 같은 입력이면 항상 같은 출력. DB 없이 테스트되고,
      나중에 LLM 툴로 감쌀 때 시그니처가 그대로 툴 스키마가 된다.

  조립 (team.py)
      repositories 로 값을 모아 순수 계산에 넘긴다. conn 을 인자로 받되
      SQL 은 쓰지 않는다.

어느 쪽이든 print()/input() 은 하지 않는다. 그건 interfaces 의 일이다.

── 아직 없는 것: 상대 팀 ──
  team.py 와 모양이 거의 같아지되 다른 점이 두 가지다.

    1. 상대 스펙은 대부분 모른다. SP·성격·도구는 추정값이고, 확정된 것과
       추정한 것을 구분해서 들고 있어야 한다. (최속/최둔 가정 등)
    2. 대전마다 새로 만들어지고 버려진다. 내 팀처럼 파일에 저장할 이유가 없다.

  그래서 team.py 를 그대로 복사하지 말고, 공통 부분이 정말 생겼을 때
  services/roster.py 같은 곳으로 뽑아내는 편이 낫다.

  예전에는 이 메모가 opponent.py 라는 빈 파일에 있었다. 코드가 0줄인 파일은
  목록에서 "있는 기능" 처럼 보여서 여기로 옮겼다.
"""

__all__ = []
