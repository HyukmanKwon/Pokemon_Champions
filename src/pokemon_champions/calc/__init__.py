"""순수 계산기 — conn 을 받지 않는다.

여기 있는 함수는 전부 같은 입력이면 같은 출력을 낸다. DB 도 안 읽고
파일도 안 쓰고 print() 도 안 한다. 필요한 값은 전부 인자로 받는다.

    stats.py       make_sp · calc_stats
    damage.py      calc_damage · analyze_ko · power_index · bulk_index
    modifiers.py   특성 · 도구 보정을 4096 정수로
    residual.py    턴 끝 지속 데미지 · 회복

── 폴더 이름이 곧 규칙이다 ──
  이 폴더에 새 파일을 넣을 자격은 하나뿐이다 — 시그니처에 conn 이 없을 것.
  conn 을 받는 순간 usecases/ 로 가야 한다.

  이 규칙을 문서가 아니라 폴더 이름에 둔 이유가 있다. 예전에는 순수 계산과
  조립이 services/ 안에 섞여 있었고, 어느 쪽에 넣을지 정하는 규칙이 없어서
  conn 을 받는 team.py 와 conn 이 없는 damage.py 가 나란히 놓였다. 그러면
  파일을 열어보기 전에는 DB 가 필요한지 알 수 없다.

── 왜 순수해야 하나 ──
  1. 검증. 데미지 공식은 실제 게임 값과 대조해서 맞춰야 한다. 순수 함수면
     scripts/check_damage.py 로 언제든 다시 재고, tests/ 에 박아둔 케이스가
     1초 만에 전부 돌아간다. 지금 테스트 103개 중 92개가 DB 없이 돈다.

  2. LLM 툴. "모델은 계산하지 않고 라우팅만 한다" 를 코드로 강제한다.
     입력이 전부 인자로 드러나 있으면 툴 스키마는 시그니처를 옮겨 적는
     수준이 된다. 함수가 안에서 DB 를 뒤지면, 이름을 잘못 넘겼을 때
     조회 실패인지 계산 실패인지 모델이 구분할 수 없다.

  3. 재현. "이 계산 왜 이렇게 나왔냐" 는 물음에 로그의 인자를 다시 넣으면
     된다.

── 배틀 중에 변하는 것은 BattleContext 한 덩어리로 ──
  날씨 · 필드 · 랭크 · 상태이상을 인자로 하나씩 늘리면 새 특성 하나 지원할
  때마다 모든 호출부를 고쳐야 한다. 확정 N타 분석은 본질적으로 "context 를
  한 턴씩 바꾸며 같은 함수를 반복 호출" 이라 상태가 한 덩어리여야 한다.

  참조표(타입 상성 · 날씨 · 필드 · 상태이상)는 Rules 로 받는다. 이것도
  같은 이유다 — 여기서 읽으면 conn 이 필요해진다.
"""

__all__ = []
